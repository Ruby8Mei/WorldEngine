# The message and ciphertext boxes on two lines, and the wider label
# column that stopped "ciphertext" being cut in a monospace face.
#
# Coordinates are logical, not the pixels in the screenshot. At the 110%
# zoom in inop.gui.json the window is 1745x982 logical.
move -5000 -5000
wait 0.5
key down
wait 0.3
key enter
wait 1.2
move 1654 57
wait 0.3
click
wait 0.6
move 1654 21
wait 0.3
click
wait 1.4
shot t1-enciphering-empty

# Enough text to run past the right edge of the box, which is where the
# old single line one started scrolling sideways and this one wraps.
move 300 166
wait 0.3
click
wait 0.2
type the quick brown fox jumps over the lazy dog while the sun goes down behind the hill and nobody says a word about any of it at all and then the second line begins here with more words to prove that the wrap is real
wait 0.4
shot t2-message-wrapped

# A click on the second row. The caret has to land on that row and not on
# the first one, which is the whole point of the row aware hit test.
move 400 195
wait 0.3
click
wait 0.4
shot t3-caret-second-row

# Encipher, then the same again in the ciphertext box below.
move 1653 166
wait 0.3
click
wait 1.0
shot t4-enciphered
quit
