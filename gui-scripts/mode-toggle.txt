# Experimental: encipher and decipher as one toggle rather than two
# sections stacked. Whichever half is on screen has the window to itself.
#
# Coordinates are logical, not the pixels in the screenshot. At the 110%
# zoom in inop.gui.json the window is 1745x982 logical.
move -5000 -5000
wait 0.5
key down
wait 0.3
key enter
wait 1.2
move 1654 57
wait 0.3
click
wait 0.6
move 1654 21
wait 0.3
click
wait 1.4
shot m1-encipher-mode

move 300 205
wait 0.3
click
wait 0.2
type the quick brown fox jumps over the lazy dog while the sun goes down behind the hill and nobody says a word about any of it at all and then the second line begins here with more words to prove that the wrap is real
wait 0.4
move 1653 205
wait 0.3
click
wait 1.0
shot m2-enciphered

# The toggle itself. One click and the other half has the window.
move 170 145
wait 0.3
click
wait 0.8
shot m3-decipher-mode
quit
